<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Battlebuff" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">Battlebuff</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <!-- <li><%= link_to "Link1", "/path1"  %></li> -->
          </ul>
          <ul class="nav navbar-nav navbar-right">
              <li><%= link_to "Login", "/auth/steam"  %></li>
            </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-3">
          <div class="well sidebar-nav">
            <h3>Steam profile</h3>
            <ul class="nav nav-list">
              <% if session.key? :current_user %>
              <li>avatar: <%= image_tag session[:current_user]['image'] %></li>
              <li>nickname: <%= session[:current_user]['nickname'] %></li>
              <li>steam_id: <%= session[:current_user]['uid'] %></li>
              <li><%= link_to "Logout", '/logout' %></li>
              <% else%>
              <li>Not logged in.</li>
              <% end %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2019</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
